#include <stdio.h>

int main()
{
	// logical operators

	// logical AND operator &&
	if(1 > 0 && 2 > 1)
		printf("Logical &&\n");

	// logical OR operator ||
	if(1 > 0 || 1 > 2)
		printf("Logical ||\n");

	// logical NOT operator !
	if(!0)
		printf("Logical !\n");

	return 0;
}
